{
  "name": "lockdown",
  "version": "0.0.0",
  "scripts": {
    "start": "preact watch --template index.html",
    "build": "preact build --no-prerender",
    "start:build": "sirv build --cors --single",
    "format": "prettier \"src/**/*.{js,jsx,css,md}\" \"package.json\" --write"
  },
  "devDependencies": {
    "preact-cli": "^3.0.0-rc.6",
    "sirv-cli": "^0.4.5",
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "bulma": "^0.8.0",
    "date-fns": "^2.11.0",
    "leaflet": "^1.6.0",
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1",
    "rslider": "^2.16.3"
  }
}
